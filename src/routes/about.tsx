import { useState } from "react";
import { createFileRoute } from "@tanstack/react-router";
import { X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { PageHero, seoMeta } from "@/components/page-hero";
import hero from "@/assets/classroom-hero.jpg"; import approach from "@/assets/academy-approach.jpg"; import wide from "@/assets/classroom-wide.jpg";
export const Route=createFileRoute("/about")({head:()=>({meta:seoMeta("About Joshi’s Academy | Kharadi, Pune","Learn about Joshi’s Academy’s concept-led approach to CBSE and ICSE Science education for Classes IX–X.")}),component:About});
function About(){const [view,setView]=useState<string|null>(null); const images=[[hero,"A small batch observing a Science demonstration"],[approach,"A teacher explaining Physics concepts"],[wide,"A focused Science classroom in session"]] as const; return <><PageHero eyebrow="Our story" title="An academy built around a simple belief: clarity changes everything." intro="Joshi’s Academy specialises in Science for CBSE and ICSE Classes IX–X, bringing personal attention and a disciplined learning structure to students in Kharadi, Pune."/><section className="section-shell grid gap-16 py-24 md:grid-cols-2"><h2 className="font-display text-5xl">Science specialisation, with the student at the centre.</h2><div className="space-y-6 leading-8 text-slate"><p>The academy’s approach begins with the concept—not the answer. Lessons are structured to help students understand relationships, ask better questions and apply their knowledge with confidence.</p><p>Small batches make it possible to notice where a student hesitates. Structured notes, regular tests, MCQs and dedicated doubt solving then create a clear rhythm of learning and improvement.</p><p>For board-year students, this foundation is paired with deliberate revision and examination practice.</p></div></section><section className="bg-muted py-24"><div className="section-shell"><p className="eyebrow text-gold">Classroom experience</p><h2 className="mt-6 font-display text-5xl">Learning, observed closely.</h2><div className="mt-14 grid auto-rows-[220px] gap-4 md:grid-cols-12">{images.map(([src,alt],i)=><button key={src} onClick={()=>setView(src)} className={`group overflow-hidden ${i===0?"md:col-span-7 md:row-span-2":"md:col-span-5"}`}><img src={src} width={i===1?1200:1600} height={i===1?1504:i===2?1008:1200} loading="lazy" className="size-full object-cover transition-transform duration-700 group-hover:scale-[1.03]" alt={alt}/><span className="sr-only">View image: {alt}</span></button>)}</div></div></section>{view&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/95 p-4" role="dialog" aria-modal="true" aria-label="Classroom photograph"><img src={view} className="max-h-[88vh] max-w-full object-contain" alt="Expanded classroom view"/><Button variant="iconLight" size="icon" className="absolute right-5 top-5" onClick={()=>setView(null)} aria-label="Close image"><X/></Button></div>}</>}