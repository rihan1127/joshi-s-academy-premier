import { createFileRoute,Link } from "@tanstack/react-router"; import { ArrowRight } from "lucide-react"; import { PageHero,seoMeta } from "@/components/page-hero"; import { articles } from "@/content/site";
export const Route=createFileRoute("/journal/")({head:()=>({meta:seoMeta("The Joshi’s Journal | Science Study Guides","Useful CBSE and ICSE Science study guidance for Classes IX–X from Joshi’s Academy, Kharadi.")}),component:Journal}); function Journal(){return <><PageHero eyebrow="The Joshi’s Journal" title="Better ways to study Science." intro="Practical, thoughtful guidance for students and parents—written to improve learning, not to chase keywords."/><section className="section-shell py-24"><div className="grid gap-px bg-border md:grid-cols-2">{articles.map((a,i)=><Link key={a.slug} to="/journal/$slug" params={{slug:a.slug}} className="group min-h-[330px] bg-ivory p-9 md:p-12"><p className="eyebrow text-gold">{a.category} • 0{i+1}</p><h2 className="mt-12 max-w-lg font-display text-4xl group-hover:text-gold">{a.title}</h2><p className="mt-5 max-w-lg leading-7 text-slate">{a.excerpt}</p><span className="mt-8 flex items-center gap-2 text-sm font-bold">Read article <ArrowRight className="size-4"/></span></Link>)}</div></section></>}